import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ordered.app',
  appName: 'Ordered',
  webDir: 'public',
  bundledWebRuntime: false
};

export default config;
